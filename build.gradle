apply plugin: 'cpp-application'
apply plugin: 'xcode'
apply plugin: 'visual-studio'

group = 'teoco.games'

allprojects {
    dependencies {
        implementation('teoco.games:app') {
	        version {
        	    branch = 'master'
        	}
        }
    }
}
 dependencies {
	compile project(":app")
}
